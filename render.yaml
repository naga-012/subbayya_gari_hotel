services:
  - type: web
    name: subbayyagar-hotel
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: GMAIL_APP_PASSWORD
        sync: false
